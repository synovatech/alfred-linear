{
  "name": "alfred-linear",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.mjs",
    "sync": "cp workflow-folder/info.plist workflow/info.plist && cp workflow-folder/icon.png workflow/icon.png",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@linear/sdk": "^86.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "*",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
