{
  "name": "alfred-linear",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@linear/sdk": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "*",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
